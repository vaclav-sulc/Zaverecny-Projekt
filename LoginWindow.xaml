<ui:FluentWindow x:Class="Zaverecny_Projekt.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="ŽlabGrade"
                 Width="400" Height="450"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ResizeMode="NoResize">
    <Grid>
        <ui:TitleBar x:Name="TitleBar" ShowMaximize="False"  />
        <TextBlock Text="ŽlabGrade" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,30,0,0"/>

        <ui:InfoBar x:Name="WarningBar" Title="Chyba přihlášení" Message="Zadané údaje nejsou správné!" Severity="Error" IsClosable="True" IsOpen="False" Margin="20,241,20,0" VerticalAlignment="Top" />
        <ui:TextBox x:Name="LoginTextBox" PlaceholderText="Uživatelské jméno" Margin="20,126,20,0" VerticalAlignment="Top" />
        <ui:PasswordBox x:Name="PasswordTextBox" PlaceholderText="Heslo" Margin="20,181,20,0" VerticalAlignment="Top" />
        <ui:Button x:Name="LoginButton" Background="Purple" Foreground="White" Appearance="Primary" Content="Přihlásit se" Margin="0,329,0,0" VerticalAlignment="Top" Click="LoginButton_Click" HorizontalAlignment="Center"/>
    </Grid>
</ui:FluentWindow>