<ui:FluentWindow x:Class="ZlabGrade.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="ŽlabGrade"
                 SizeToContent="Manual"
                 MinWidth="400"
                 Width="400" Height="450"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ResizeMode="NoResize">
    <Grid>
        <ui:TitleBar x:Name="TitleBar" ShowMaximize="False"/>
        
        <TextBlock Text="ŽlabGrade" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,50,0,0"/>

        <ui:TextBox x:Name="LoginTextBox" PlaceholderText="Uživatelské jméno" Margin="20,170,20,0" VerticalAlignment="Top"/>
        <ui:PasswordBox x:Name="PasswordBox" PlaceholderText="Heslo" Margin="20,220,20,0" VerticalAlignment="Top"/>

        <ui:InfoBar x:Name="WarningBar" Title="Chyba přihlášení" Message="Zadané údaje nejsou správné!" Severity="Error" IsClosable="False" IsOpen="False" Margin="20,275,20,0" VerticalAlignment="Top"/>

        <ui:Button x:Name="LoginButton" Content="Přihlásit se" Appearance="Secondary" Margin="0,360,0,0" Width="300" Height="35" VerticalAlignment="Top" HorizontalAlignment="Center" Background="Purple" Foreground="White" FontWeight="Bold" Click="LoginButton_Click"/>
    </Grid>
</ui:FluentWindow>